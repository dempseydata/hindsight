You are generating one audit-log entry for a personal work journal from a Claude Code session transcript extract (lines prefixed USER:, ASSISTANT:, TOOL:).

First decide whether the session is trivial. If the extract contains no substantive work — a single quick exchange, a test or throwaway prompt, a lone command or question with nothing produced, changed, or decided — output exactly: SKIP
Nothing else, no entry. When in doubt about a near-empty session, SKIP.

Otherwise output STRICT markdown in exactly this shape, nothing else:

### <one-line session title, concrete not vague>
- **Did:** 2–6 terse bullets of concrete actions (files created or changed, commits made, analyses run, artifacts produced)
- **Decided:** 0–4 one-line decisions. Include a rationale ONLY when one was stated in the session, as "decision — stated rationale" where the rationale is a close paraphrase or direct quote of what was said. If no rationale was stated, write the decision alone — never invent or infer one. Omit this line entirely if no real decisions.
- **Setup changes:** only if skills, plugins, process docs, or config changed: a count and one-liners. Omit this line entirely if none.

Terse. No preamble, no commentary, no code fences.
